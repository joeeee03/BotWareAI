# Variables de Entorno para Railway
# Copia estas variables en la sección "Variables" de tu servicio en Railway

# ===== REQUERIDAS =====

# Base de datos PostgreSQL (Railway la genera automáticamente al agregar PostgreSQL)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# JWT Secret (genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu-jwt-secret-muy-seguro-de-minimo-32-caracteres

# Encryption Key - CRÍTICO: Usa la MISMA clave que usas localmente
# Esta clave se usa para encriptar/desencriptar mensajes de chat y datos sensibles
# IMPORTANTE: Si cambias esta clave, NO podrás desencriptar datos antiguos
ENCRYPTION_KEY=d9G4kPq7V2sH6nZ1xR8bT3wL0yF5cM2a

# Entorno de producción
NODE_ENV=production

# ===== OPCIONALES (WhatsApp Integration) =====

# Solo si usas integración con WhatsApp Business API
# META_API_TOKEN=tu_meta_api_token
# META_BUSINESS_ACCOUNT_ID=tu_business_account_id
# META_PHONE_NUMBER_ID=tu_phone_number_id

# ===== NO CONFIGURAR EN RAILWAY (automáticas) =====

# Estas variables NO deben configurarse manualmente en Railway:
# - PORT (Railway lo asigna automáticamente)
# - FRONTEND_URL (se configura internamente en el servidor)
# - NEXT_PUBLIC_BACKEND_URL (se configura internamente en server-combined.js)
# - HOSTNAME (configurado en server-combined.js)
